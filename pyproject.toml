[project]
name = "yaya"
version = "0.1.0"
description = "Yet Another YAML AST transformer - Byte-for-byte preserving YAML editor"
authors = [
    { name = "Ryan Williams", email = "ryan@no.de" }
]
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "ruamel.yaml>=0.17.0",
]
license = { text = "MIT" }
keywords = ["yaml", "ast", "transformer", "editor", "lossless", "round-trip", "formatting"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.8",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Software Development :: Libraries :: Python Modules",
    "Topic :: Text Processing :: Markup",
]

[project.urls]
Homepage = "https://github.com/Open-Athena/yaya"
Repository = "https://github.com/Open-Athena/yaya"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/yaya"]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = "test_*.py"
